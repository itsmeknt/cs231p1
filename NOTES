1. in modelinit.m, we can change the number of components (perspectives) for each part
centered filters constrained to be symmetric. [2010 section 5.1]. We can remove this constraint
latent root filters - retrain multiple times, maybe root filter will get better and closer to the original